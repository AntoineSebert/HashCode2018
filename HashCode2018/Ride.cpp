#include "Ride.h"

Ride::Ride(intersection start, intersection finish, unsigned int earliest, unsigned int latest) : start(start), finish(finish), earliest(earliest), latest(latest) {}

std::ostream& Ride::operator<<(std::ostream& os) {

}
